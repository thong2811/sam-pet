<?php
/**
 * @var Laminas\View\Renderer\PhpRenderer $this
 */

use Application\Service\CsrfService;

$successMessages = $this->flashMessenger()->getSuccessMessages();
$errorMessages   = $this->flashMessenger()->getErrorMessages();
$infoMessages    = $this->flashMessenger()->getMessages();
$csrfToken       = CsrfService::getToken();

// Active nav: lấy path hiện tại để highlight đúng menu item
$currentPath = '/' . ltrim($_SERVER['REQUEST_URI'] ?? '', '/');

// Nhóm navbar dropdown — mỗi nhóm gồm label, icon, href prefix để detect active, và danh sách items
$navGroups = [
    [
        'label' => 'Kho hàng',
        'icon'  => '🏪',
        'prefixes' => ['/product', '/import-stock', '/export-stock', '/stocktaking'],
        'items' => [
            ['href' => '/product',      'label' => 'Quản lý kho'],
            ['href' => '/import-stock', 'label' => 'Nhập hàng'],
            ['href' => '/export-stock', 'label' => 'Xuất hàng'],
            ['href' => '/product/repackage',  'label' => 'Chiết hàng'],
            ['href' => '/stocktaking',  'label' => 'Kiểm kê'],
            ['href' => '/product/add-invoice-check',  'label' => 'Trạng thái hóa đơn'],
        ],
    ],
    [
        'label' => 'Phòng khám',
        'icon'  => '🏥',
        'prefixes' => ['/owner-pet', '/medical-record', '/vet-care'],
        'items' => [
            ['href' => '/vet-care',        'label' => 'Điều trị - Spa'],
            ['href' => '/owner-pet',       'label' => 'Vật nuôi'],
            ['href' => '/medical-record',  'label' => 'Khám bệnh'],
        ],
    ],
    [
        'label' => 'Tài chính',
        'icon'  => '📊',
        'prefixes' => ['/overview', '/report', '/export-invoice', '/expenses'],
        'items' => [
            ['href' => '/overview',        'label' => 'Tổng quan'],
            ['href' => '/expenses',        'label' => 'Quản lý chi'],
            ['href' => '/report',          'label' => 'Báo cáo thu - chi'],
            ['href' => '/export-invoice',  'label' => 'Sổ doanh thu'],
        ],
    ],
];

// Helper: kiểm tra nhóm có active không
$isGroupActive = function(array $prefixes) use ($currentPath): bool {
    foreach ($prefixes as $prefix) {
        if (str_starts_with($currentPath, $prefix)) return true;
    }
    return false;
};

// Tiêu đề trang: ưu tiên headTitle được set bởi view, fallback về STORE_NAME
$storeName = htmlspecialchars($_ENV['STORE_NAME'] ?? 'Sam Pet', ENT_QUOTES, 'UTF-8');
?>
<?= $this->doctype() ?>
<html lang="vi">
    <head>
        <meta charset="utf-8">
        <meta name="csrf-token" content="<?= $this->escapeHtmlAttr($csrfToken) ?>">
        <?= $this->headTitle($storeName)->setSeparator(' - ')->setAutoEscape(false) ?>

        <?= $this->headMeta()
            ->appendName('viewport', 'width=device-width, initial-scale=1.0')
            ->appendHttpEquiv('X-UA-Compatible', 'IE=edge')
        ?>

        <!-- Styles: Bootstrap trước, style.css sau để override đúng thứ tự cascade -->
        <?= $this->headLink([
                'rel'  => 'shortcut icon',
                'type' => 'image/vnd.microsoft.icon',
                'href' => $this->basePath() . '/img/favicon.ico',
            ])
            ->appendStylesheet($this->basePath('css/bootstrap.min.css'))
            ->appendStylesheet($this->basePath('css/style.css'))
            ->appendStylesheet('https://cdn.datatables.net/2.1.8/css/dataTables.dataTables.min.css')
            ->appendStylesheet('https://cdn.datatables.net/select/2.1.0/css/select.dataTables.min.css')
            ->appendStylesheet('https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css')
            ->appendStylesheet('https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.10.0/css/bootstrap-datepicker.min.css')
        ?>

        <!-- Scripts -->
        <?= $this->headScript()
            ->appendFile('https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js')
            ->appendFile('https://code.jquery.com/jquery-3.7.1.min.js')
            ->appendFile('https://cdn.datatables.net/2.1.8/js/dataTables.min.js')
            ->appendFile('https://cdn.datatables.net/plug-ins/2.1.8/sorting/date-eu.js')
            ->appendFile('https://cdn.datatables.net/select/2.1.0/js/dataTables.select.min.js')
            ->appendFile('https://cdn.datatables.net/select/2.1.0/js/select.dataTables.min.js')
            ->appendFile($this->basePath('js/custom.dataTables.js'))
            ->appendFile('https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js')
            ->appendFile('https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.10.0/js/bootstrap-datepicker.min.js')
            ->appendFile($this->basePath('js/common.js'))
        ?>
    </head>
    <body>
        <!-- Flash messages: thêm padding-top để không che nội dung -->
        <div class="flash-messages" id="flashMessages">
            <?php if (!empty($successMessages)): ?>
                <?php foreach ($successMessages as $message): ?>
                    <div class="alert alert-success m-0 p-1">
                        <?= $this->escapeHtml($message) ?>
                        <button type="button" class="btn-close float-end" onclick="closeAlertMessage(this)"></button>
                    </div>
                <?php endforeach; ?>
            <?php endif; ?>
            <?php if (!empty($errorMessages)): ?>
                <?php foreach ($errorMessages as $message): ?>
                    <div class="alert alert-danger m-0 p-1">
                        <?= $this->escapeHtml($message) ?>
                        <button type="button" class="btn-close float-end" onclick="closeAlertMessage(this)"></button>
                    </div>
                <?php endforeach; ?>
            <?php endif; ?>
            <?php if (!empty($infoMessages)): ?>
                <?php foreach ($infoMessages as $message): ?>
                    <div class="alert alert-info m-0 p-1">
                        <?= $this->escapeHtml($message) ?>
                        <button type="button" class="btn-close float-end" onclick="closeAlertMessage(this)"></button>
                    </div>
                <?php endforeach; ?>
            <?php endif; ?>
        </div>

        <nav class="navbar navbar-expand-lg navbar-dark" id="mainNav">
            <div class="container-fluid">
                <a class="navbar-brand" href="/"><?= $storeName ?></a>
                <button class="navbar-toggler" type="button"
                        data-bs-toggle="collapse" data-bs-target="#navbarMain"
                        aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarMain">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <?php foreach ($navGroups as $gi => $group):
                            $groupActive = $isGroupActive($group['prefixes']);
                            $dropdownId  = 'navDropdown' . $gi;
                        ?>
                        <li class="nav-item dropdown<?= $groupActive ? ' active' : '' ?>">
                            <a class="nav-link dropdown-toggle<?= $groupActive ? ' active' : '' ?>"
                               href="#"
                               id="<?= $dropdownId ?>"
                               role="button"
                               data-bs-toggle="dropdown"
                               aria-expanded="false">
                                <?= $group['icon'] ?> <?= $group['label'] ?>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="<?= $dropdownId ?>">
                                <?php foreach ($group['items'] as $item):
                                    $itemActive = str_starts_with($currentPath, $item['href']);
                                ?>
                                <li>
                                    <a class="dropdown-item<?= $itemActive ? ' active' : '' ?>"
                                       href="<?= $item['href'] ?>"
                                       <?= $itemActive ? 'aria-current="page"' : '' ?>>
                                        <?= $item['label'] ?>
                                    </a>
                                </li>
                                <?php endforeach; ?>
                            </ul>
                        </li>
                        <?php endforeach; ?>

                        <!-- Cài đặt — item đơn, không dropdown -->
                        <?php $settingsActive = str_starts_with($currentPath, '/settings'); ?>
                        <li class="nav-item<?= $settingsActive ? ' active' : '' ?>">
                            <a class="nav-link<?= $settingsActive ? ' active' : '' ?>"
                               href="/settings"
                               <?= $settingsActive ? 'aria-current="page"' : '' ?>>
                                ⚙ Cài đặt
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

        <div class="container main-content">
            <?php
            // Auto sub-nav: tìm nhóm hiện tại và render sub-nav tự động
            // View không cần tự viết sub-nav nữa
            $activeGroup = null;
            foreach ($navGroups as $group) {
                if ($isGroupActive($group['prefixes'])) {
                    $activeGroup = $group;
                    break;
                }
            }
            if ($activeGroup):
            ?>
            <nav class="sub-nav d-flex flex-wrap gap-2 py-2 px-1 mb-3">
                <?php foreach ($activeGroup['items'] as $item):
                    $itemActive = str_starts_with($currentPath, $item['href']);
                ?>
                <a href="<?= $item['href'] ?>"
                   class="btn btn-sm <?= $itemActive ? 'btn-light text-dark fw-bold' : 'btn-outline-light' ?>">
                    <?= $item['label'] ?>
                </a>
                <?php endforeach; ?>
            </nav>
            <?php endif; ?>

            <?= $this->content ?>
            <hr>
            <footer>
                <p>&copy;<?= date('Y') ?> <?= $storeName ?></p>
            </footer>
        </div>

        <!-- Overlay loading — text thay đổi theo context qua JS -->
        <div class="overlay-loading" id="overlayLoading">
            <div class="spinner-border text-info"></div>
            <div class="text-white fw-bold ms-3 overlay-loading-text" style="font-size: 14px;">Đang xử lý...</div>
        </div>

        <!-- Calculator widget: chỉ hiện khi cần (toggle bằng class .show-calculator) -->
        <div class="calculate-container">
            <button class="btn btn-warning opacity-50" type="button"
                    data-bs-toggle="collapse" data-bs-target="#calculate"
                    aria-expanded="false" aria-controls="calculate">
                Máy tính
            </button>
            <div class="collapse" id="calculate">
                <div class="card card-body w-100">
                    <input type="text" id="calculateExpression"
                           oninput="loadResultCalculate()"
                           placeholder="Nhập biểu thức (vd: 150000*3+50000)">
                    <div>= <span id="calculateResult"></span></div>
                </div>
            </div>
        </div>

        <script>
            // Đặt padding-top cho body bằng chiều cao flash-messages để không bị che
            (function adjustBodyPadding() {
                const flash = document.getElementById('flashMessages');
                if (flash && flash.children.length > 0) {
                    document.body.style.paddingTop = flash.offsetHeight + 'px';
                }
            })();
        </script>
    </body>
</html>
